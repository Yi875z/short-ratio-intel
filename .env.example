# ============================================================
# short-ratio-intel 環境変数設定ファイル
# このファイルをコピーして .env を作成し、各値を入力してください
# .env は絶対にGitにコミットしないこと
# ============================================================

# --- J-Quants API (v2) ---
# v2 は x-api-key ヘッダ方式。マイページで発行するキーをそのまま入れる
# （有効期限なし）。v1 のメール+パスワードによるトークン交換は不要になった。
#
# 用途は騰落銘柄数・TOPIX騰落率・取引カレンダー。空売り比率そのものは
# Light プランでは 403（Standard 以上）なので、従来どおり JPX 公開PDF から取る。
# 未設定でもアプリとパイプラインは動く（騰落銘柄数が「未取得」になるだけ）。
JQUANTS_API_KEY=your_jquants_api_key
# 以下は v1 時代の名残。v2 では使わない。
# JQUANTS_EMAIL=your_email@example.com
# JQUANTS_PASSWORD=your_password

# --- Gemini API ---
GEMINI_API_KEY=your_gemini_api_key
# 使用モデルは config/settings.py の GEMINI_MODEL_DEFAULT が正本。
# 下の3つは緊急避難用の上書きなので、通常はコメントアウトのままにする
# （設定すると起動ログと Streamlit 画面に「上書き中」の警告が出る）。
# GEMINI_MODEL=gemini-3.6-flash
# GEMINI_FALLBACK_MODELS=gemini-3.6-flash,gemini-3.5-flash
# GEMINI_REQUEST_TIMEOUT_SEC=300

# --- オプション: Slack通知 ---
SLACK_WEBHOOK_URL=

# --- オプション: ニュース検索API (Step 0用) ---
TAVILY_API_KEY=

# --- アプリ設定 ---
LOG_LEVEL=INFO
DATA_DIR=data
DB_PATH=data/short_ratio.db
REPORTS_DIR=data/reports
