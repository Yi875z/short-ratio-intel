# 2026-07-03 ハンドオフ — 市場イベント・カレンダー全面刷新

## 目的

カレンダーのイベント期日の誤りと網羅不足を解消するため、
`C:\CarSol\JPX_Analysis_System\core\event_calendar.py`（公式日程キュレーション済み）を
参考に、short-ratio-intel のイベントカレンダーを全面刷新した。

## 変更ファイル

- `config/market_calendar.py` — 2026年公式確定日へ全面更新。
  追加: 米CPI通年（旧版はH2未収録）/ 米PPI / 米雇用統計（NFP）/ 米PCE / 米GDP /
  米小売売上高 / 米JOLTS / 日本CPI全国（公式日）/ 東京CPI / 日銀12月会合（12/18、旧版欠落）/
  米中間選挙（11/3）。指標ごとの辞書＋メタ表から `CURATED_EVENTS` を生成する構造に変更。
- `src/macro_context/event_calendar.py` —
  - `MarketEvent` に `phase` フィールド追加（発表/実売買/発効/基準/注意）。
  - 誤日付の温床だった「目安計算」（NFP=第1金曜、米PPI=2週目木曜、日本CPI=第4金曜）を廃止し
    キュレーション公式日へ移行。
  - 指数リバランス6系統を新規追加: MSCI / FTSE GEIS / 日経平均定期入替 /
    TOPIX（定期＋移行措置2026-2028）/ JPX日経400 / JPX Prime150（2026年は公式確定日、
    他年はルール計算フォールバック）。
  - 配当権利落ち日を修正: 旧「月末最終営業日」→ 正しくは権利確定日の1営業日前
    （2026年3月は3/30、9月は9/29）。
  - 追加: 米ISM製造業/サービス業（第1・第3米国営業日、連邦祝日考慮）、日銀短観、
    日本企業物価（第8営業日目安・low）、GPIFウォッチ（low）。
  - プロンプトブロックは low 重要度を除外し30行キャップ、フェーズラベル付き。
- `app/streamlit_app.py` — カレンダーチップをフェーズ別配色に変更
  （橙=実売買、緑=発効、灰=発表/基準、紫=SQ/ロール、赤=日本、青=米国）。凡例・説明文更新。
  サイドバー一覧に フェーズ表示追加・low重要度除外。
- `tests/test_event_calendar.py` — 17テストへ拡充（公式日検証・フェーズ・配当落ち日・
  祝日調整・2027フォールバック等）。
- `PROJECT_RULES.md` — テスト基準を 52→63件に更新。
- `src/ai_engine/gemini_client.py` — 429（クォータ超過）時のリトライ待機を 1〜4秒→65秒に変更。
  無料枠のRPM(5/分)・入力トークン(250K/分)枠がリセットされる前に再試行して全滅していた
  2026-06-26〜07-02 の連続失敗への恒久対策。

## 日付検証の根拠（外部確認済み）

- 米CPI 1月分 = 2026-02-13（BLSアーカイブ `cpi_02132026.htm` で確認。旧版の2/11は誤り）
- 米雇用統計 1月分 = 2026-02-11（政府閉鎖で2/6→2/11に変更された実績日。BLS/DOLで確認）
- その他の日程は JPX_Analysis_System の公式キュレーション（本番稼働中）から移植。

## 実行コマンドとテスト結果

- `python -m pytest tests/ -q` → **63 passed**（変更前は52 passed）
- `python -m py_compile` → 3ファイルすべてOK
- 秘密情報スキャン（手動grep）→ 混入なし（gitleaks未インストールのため手動）

## 未解決事項

- **未確認**: 日本GDP 1次速報の2026年8月・11月分の公表日（内閣府未発表のため未収録）。
- **未確認**: 日銀短観12月調査の公表日（12/14は目安。watch扱いで明示済み）。
- 日本の祝日は簡易判定（土日除外のみ）。2026年の月末・SQ・指数イベントには影響しないことを
  確認済みだが、2027年以降のフォールバック日程は目安。
- ローカルでは動作確認済みだが、**本番（Streamlit Cloud）への push は未実施**。
  push すると自動デプロイされる。

## ロールバック方法

`git revert` で該当2コミットを戻す（データ・スキーマ変更なし、コードのみ）。

## 次の一手

1. `git push` で本番反映（ユーザー承認後）。
2. 2026年H2に日本GDP・短観12月の公式日が出たら `config/market_calendar.py` に追記。
3. 年次更新（2027年分）: FOMC/日銀/BLS/BEA/総務省の公式カレンダー公表後に
   `config/market_calendar.py` と `event_calendar.py` の2026固定日程を更新。
