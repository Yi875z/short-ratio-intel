# 2026-07-04 ハンドオフ — AIレポートへのVaultナレッジ統合（提案1〜5実装）

## 目的

Obsidian Vault（C:\Obsidian\NEO-Financial-Advisor\01_Knowledge）を基準にした
AIレポートレビューの改善提案1〜5を実装した。

## 変更ファイル

- `src/ai_engine/prompt_builder.py`
  - ①00プロトコルの「先頭12,000字クリップ」を廃止し、分析ルール9セクション
    （事実/推測分離・JPX禁止表現・投資主体別時間差・J-NET判定・GEX・マクロ・
    テクニカル・資金フロー・心理）だけを見出し抽出する `_extract_protocol_digest` に変更。
    見出し構成が変わったら従来クリップへフォールバック。
  - ②システムプロンプトに 06 ユーザー固有ルール（user_rules・9,000字上限）を注入。
    SQ・MSQ週（対象日の-2〜+5日にSQ/ロールがある場合）のみ 07 過去事例を
    ユーザープロンプトへ条件注入する `_build_sq_week_case_block` を追加（通常日は注入せず
    クォータを守る）。テーマの資金フロー4分類＋Unconfirmed の記述ルールを鉄則に追加。
  - ③`executive_summary` / `regime` の出力指示を追加。
  - ④事実:/解釈:/推測: ラベル分離ルールのセクションを追加。
- `src/ai_engine/output_schema.py` — `executive_summary`・`regime`（ReadingReport）、
  `flow_classification`（DominantMarketTheme、既定 Unconfirmed）を追加。
  いずれも default 付きで過去保存分のJSONと後方互換。
- `src/ai_engine/gemini_client.py` — Markdown冒頭に「🧭 本日の結論／レジーム判定」、
  テーマに「フロー区分」を描画。`_parse_response` に setdefault 追加。
- `src/knowledge/loader.py` — 配信キーに `user_rules`（06）・`past_cases`（07）を追加。
- `src/storage/db.py` — `get_knowledge_document_meta()`（key・文字数・updated_at）を追加。
- `app/streamlit_app.py` — ⑤サイドバーに「🧠 ナレッジ鮮度」expander。Supabaseの
  updated_at を表示し、ローカル原本と文字数が違えば⚠️＋再アップロード警告。
- `.env`（非追跡）— `EXTERNAL_KNOWLEDGE_DIR` を Vault 01_Knowledge へ変更。
  以後 `C:\CarSol\knowledgefile` への複製は不要（レガシー化）。
- `tests/test_prompt_builder_knowledge.py` — 新規6テスト。
- `PROJECT_RULES.md` — テスト基準 63→69件。

## 実行コマンドとテスト結果

- `python -m pytest tests/ -q` → **69 passed**
- スモーク: システムプロンプト67,252字。00ダイジェストにMode定義が混入しないこと、
  06注入・SQ週条件注入（7/8=注入あり、7/21=なし）・Markdown新セクション描画を確認。
- `python -m scripts.upload_knowledge_to_supabase` → 8キー登録
  （user_rules 5,952字・past_cases 5,855字を新規配信）。

## 未解決事項

- **未確認**: 新フィールドの実生成品質（次回のレポート生成で `executive_summary`・
  `regime`・ラベル分離・フロー区分が期待どおり埋まるかの実地確認が必要）。
- Streamlit Cloud 反映には main への push が必要（未 push）。
- report_quality（品質チェック）に新フィールドの検査は未追加（様子見後に追加検討）。

## ロールバック方法

- コードは該当コミットを `git revert`。スキーマ新フィールドは default 付きのため
  巻き戻しても保存済みJSONの再表示に影響なし。
- `.env` の `EXTERNAL_KNOWLEDGE_DIR` 行を削除すれば旧パス（CarSol\knowledgefile）に戻る。
- Supabase の user_rules / past_cases は残っても既存コードからは参照されないため無害。

## 次の一手

1. main へ push → Streamlit Cloud 反映 → 次回レポートで新セクションの品質確認。
2. 品質が安定したら report_quality に「regime 3値チェック」「ラベル分離チェック」を追加。
3. 7/10（金）マイナーSQ週で 07 過去事例注入の実地効果を確認。
