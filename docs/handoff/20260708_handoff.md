# ハンドオフ 2026-07-08 — 改善計画フェーズ1 実装完了

## 概要
`docs/improvement_plan_20260706.md` のフェーズ1（即効・低リスク・依存なし）を全4タスク実装した。

## 変更ファイル一覧
- `scripts/fetch_short_ratio.py`（1-1）: `_step_report` が `(文字数, report_obj)` を返すよう変更。
  `_format_report_highlights()` を新設し、Slack通知に `regime`・`executive_summary`・
  `new_signal_summary` の要点を追記。`report_obj=None`（--no-report）でも壊れない fail-soft。
- `src/ai_engine/prompt_builder.py`（1-3）: 主要4ナレッジ（global_macro/jpx_micro/options_gex/
  quant_psych）にも `_clip(_KNOWLEDGE_CLIP_CHARS=12000)` を適用。`build_system_prompt` /
  `build_user_prompt` の末尾で文字数を `logger.info("プロンプト規模: system=/user=...")` に出力。
- `src/macro_context/market_quotes.py`（1-2）: `_fetch_nikkei_vi_quote()` /
  `_parse_smd_number()` / `_insert_nikkei_vi()` を新設。日経VIを stock-marketdata.com/vi.html
  から取得し、`fetch_quotes()` でVIX（ticker=621）直後へ配置。fail-soft。
- `app/streamlit_app.py`（1-2）: 市場データタブの出所キャプションに「日経VIのみ stock-marketdata.com」を追記。
- `tests/test_market_quotes_nikkei_vi.py`（1-2・新規）: 数値解析・VI配置の決定論テスト3件。
- `PROJECT_RULES.md`: テスト基準 69→72件、最終更新日を更新。

## 実行コマンド
- `python -m pytest -q` → **全72件パス**（従来69＋新規3、実測約8秒）。

## タスク別の受け入れ確認
- **1-1**: 通知サマリーに結論・レジーム・新規シグナルが載る実装を確認（`_format_report_highlights`）。
  実通知の目視は次回の平日19時自動実行 or 手動 workflow_dispatch 時に確認する。
- **1-2**: `_fetch_nikkei_vi_quote()` を実行 → 日経VI=29.70 / 前日比-7.66(-20.50%) / as_of=2026-07-07 を取得。
  stock-marketdata.com/vi.html の公表値と**複数日フィンガープリント照合済み**
  （07-06:37.36, 07-03:34.11, 07-02:24.74 等が一致）。金利・リスク群の並びは
  [米10年, 米30年, VIX, 日経VI, WTI, 金] でVIX直後に配置されることを確認。
  ※ nikkei225jp.com は日経VIの日次系列を持たない（S600番台・S110番台を実値スキャンし一致コードなし）
  ため別ソースを採用した。
- **1-3**: `build_system_prompt()` 実行でログ `プロンプト規模: system=67,252字` を確認。
  現状4ナレッジはいずれも12,000字未満のため実クリップは未発生（将来の肥大に対する予防線）。
- **1-4（検証のみ・コード変更なし）**: 今週 2026-07-10 は7月限マイナーSQ。`_build_sq_week_case_block()`
  が **07-06〜07-10 の全日で発火**し、過去事例07（約5,920字）が注入されることを確認。
  2026-07-04 実装が本番タイミングで初稼働することを確認できた。

## 未解決事項・未確認
- **1-1 の実通知本文**は未目視（次回自動実行で確認）。
- **1-4 の生成レポート本文へのSQ反映**は未確認。Gemini Free Tier 20 RPD をデバッグで焼かない方針のため、
  07-08〜07-10 に自動生成される日次レポートで「SQ前の機械的フロー言及」が出るかを観察して確認する。
- 日経VIの取得は stock-marketdata.com のHTML構造（data-x/data-y テーブル）に依存。構造変更時は
  `_fetch_nikkei_vi_quote()` の正規表現を要調整（fail-soft なのでアプリは停止しない）。

## ロールバック方法
- フェーズ1は3コミット（76441c4=1-1 / b4791a7=1-3 / bf03875=1-2）。個別 `git revert <hash>` で戻せる。
  いずれも fail-soft・最小差分で既存フローを壊さない。

## 次の一手
- フェーズ2（解釈力の本丸）: 2-1 業種別騰落率との4象限突合【最重要】→ 2-2 先物・オプション建玉突合 →
  2-3 テーマ採点の鮮度減衰。2-1 は業種別騰落率の取得元確定（stock-marketdata.com 第一候補）が起点。
- push 後は Streamlit Cloud 再デプロイ完了を待ってから動作確認（旧コンテナ生成レポート誤認の前例あり）。
