# ハンドオフ 2026-08-07 — 米国ショートフロー US-P2 実装完了

前回: `docs/handoff/20260806_handoff.md`（US-P1）／計画書: `docs/us_flow_integration_plan_20260806.md`

## 完了したこと

US-P2（4象限分類・バスケット/ETF乖離・日次自動実行・Streamlitタブ）を実装し、
**本番Supabaseで通しの動作確認まで完了**した。日本側のロジック・UIは無変更（追記のみ）。

### 新規ファイル

- `src/analyzer/us_flow_classifier.py` — 4象限分類。`INSUFFICIENT_DATA` を独立させ、
  「シグナルなし」と「判定不能」を混同しない。SQUEEZE_BUILDING は銘柄別に連続日数を数える
- `src/analyzer/us_basket.py` — ボリューム加重バスケット比率、ETF乖離（divergence）
- `src/report/us_daily_report.py` — Markdown日次レポート（AI不使用・Geminiクォータを消費しない）
- `scripts/fetch_us_short_flow.py` — 日次パイプライン（取得→保存→レポート→Slack）
- `.github/workflows/us_daily_fetch.yml` — 平日 08:37 JST（UTC 23:37 日〜木）
- `tests/test_us_flow_classifier.py`（10件）/ `test_us_basket.py`（8件）/ `test_us_daily_report.py`（7件）

### 既存ファイルへの追記のみ

- `app/streamlit_app.py` — 末尾に「🇺🇸 米国ショート」タブを追加（154行追加・1行変更）。
  既存タブの並びは変えていない。米国データが無くてもタブ内で案内して終わる
- `src/storage/db.py` — 米国テーブルの保存をまとめ書き込み（bulk）に変更

## 実行結果（すべて実測）

```
pytest                                       # 全145件パス / 8秒
python -m scripts.backfill_us_short_flow --days 250   # 本番Supabaseへ 26秒
python -m scripts.fetch_us_short_flow --no-slack      # 本番で通し実行 成功
```

**本番Supabase**: `us_short_volume_daily` 5,976行 / 249営業日 / 24銘柄（2025-08-11〜2026-08-06）、
`us_market_daily` 5,976行。キー重複0件。FINRAの小数精度も保持（NVDA 8/5 = 22736741.981631）。
**RLSは全8テーブルで有効・FORCEDなし**をデータ投入後にも再確認済み。

日本側は無傷: `short_ratio_daily` 2,666行・最新2026-08-06（19:07の定時実行も正常）。

### 保存性能

1行ずつ SELECT+INSERT ではSupabaseへの往復遅延（実測51ms）が支配的で、5,976行に約20分かかる
見積もりだった。まとめ書き込みへ変更して**26秒**に短縮。

### 実データで観測された挙動

8/5 は SMH乖離 −4.15（個別選別型の候補）だったが、8/6 は −0.48 に収束した。
同じ指標が日々動くことを確認できており、固定的な解釈をしない設計が効いている。
8/6 のアラートは LRCX +2.36 / ADI −2.38（LONG_LIQUIDATION候補）/ ASML −2.50 / NXPI −2.29。

## ⚠️ 未完了・要対応

**未push。** PROJECT_RULES に従いコミットのみで保留している。
**push するまで GitHub Actions の日次実行は始まらない**（ワークフロー定義がリモートに無いため）。
Streamlit Cloud の米国タブも push 後の自動再デプロイで反映される。

**Slack通知は未検証。** 実装は日本側 `_notify_slack` と同じ方式だが、実際の送信は行っていない
（外部への送信のため）。検証するなら `python -m scripts.fetch_us_short_flow` を
`--no-slack` なしで1回叩くか、push後に Actions の workflow_dispatch を手動実行する。

## 未確認事項

- GitHub Actions ランナーから FINRA CDN / Yahoo chart API を叩けるか（ローカル疎通のみ）。
  初回の自動実行で確認する。FINRA は米国からのアクセスなので問題は出にくい見込み。
- 米国祝日に 08:37 JST の実行が空振りする挙動は、日曜指定で実測済み（正常終了・Actionsは赤くならない）。
  実際の米国祝日での挙動は初回遭遇時に確認する。
- Short Interest（隔週残高）の無料取得経路 — US-P3 着手時に調査。

## ロールバック方法

コミット単位で `git revert` すれば日本側に影響なく戻せる。
本番データを消す場合のみ `DELETE FROM public.us_short_volume_daily; DELETE FROM public.us_market_daily;`
（テーブル自体を落とす必要はない）。

## 次の一手

- push（→ Actions と Streamlit Cloud が有効化される）
- JPフェーズ2-1（業種別騰落率との4象限突合）。改善計画書で最重要のまま残っている
- US-P3（Short Interest取り込み・CONFIRMED昇格・日本側AIレポートへの米国文脈注入）
